/** @type {import('next').NextConfig} */
const nextConfig = {
  trailingSlash: true,
  images: {
    unoptimized: false,
  },
};

module.exports = {
  eslint: {
    ignoreDuringBuilds: true
  }
  // keep any other existing configuration
};

